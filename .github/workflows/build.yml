# Stardew Valley Cocos2d-x 构建工作流

name: Build Stardew Valley Cocos2d-x

on:
  push:
    branches: [ main, master, develop ]
  pull_request:
    branches: [ main, master, develop ]
  # 允许手动触发工作流
  workflow_dispatch:

jobs:
  # Windows 构建任务
  windows-build:
    runs-on: windows-2022
    
    steps:
    # 检出代码（包含子模块）
    - name: 检出代码
      uses: actions/checkout@v4
      with:
        submodules: recursive
        fetch-depth: 0
        
    # 尝试设置 Python 2.7（备用方案）
    - name: 设置 Python 2.7
      uses: actions/setup-python@v4
      with:
        python-version: '2.7'
      continue-on-error: true
      id: python27
        
    # 如果 Python 2.7 失败，使用 Python 3 并修改脚本
    - name: 设置 Python 3（备用方案）
      if: steps.python27.outcome == 'failure'
      uses: actions/setup-python@v4
      with:
        python-version: '3.8'
        
    # 验证 Python 版本
    - name: 验证 Python 环境
      run: |
        python --version
        python -c "import sys; print('Python path:', sys.executable)"
        
    # 修改 download-deps.py 以支持 Python 3（如果需要）
    - name: 修改 download-deps.py 支持 Python 3
      if: steps.python27.outcome == 'failure'
      run: |
        cd cocos2d
        # 备份原文件
        copy download-deps.py download-deps.py.backup
        # 使用 PowerShell 修改文件，移除 Python 版本检查
        (Get-Content download-deps.py) | ForEach-Object {
          if ($_ -match "if not _check_python_version") {
            "    # if not _check_python_version():"
          } elseif ($_ -match "exit\(\)") {
            "    #     exit()"
          } else {
            $_
          }
        } | Set-Content download-deps.py
      shell: powershell
        
    # 下载 cocos2d-x 第三方依赖
    - name: 下载 cocos2d-x 依赖
      run: |
        cd cocos2d
        python download-deps.py --r no
        
    # 设置 Visual Studio 环境
    - name: 设置 Visual Studio 开发环境
      uses: microsoft/setup-msbuild@v1.3
      with:
        vs-version: '17.0'
        
    # 缓存 cocos2d-x 依赖
    - name: 缓存 cocos2d-x 依赖
      uses: actions/cache@v4
      with:
        path: |
          cocos2d/external/
          cocos2d/tools/
        key: ${{ runner.os }}-cocos2dx-deps-${{ hashFiles('cocos2d/download-deps.py') }}
        restore-keys: |
          ${{ runner.os }}-cocos2dx-deps-
        
    # 创建构建目录
    - name: 创建构建目录
      run: |
        mkdir build
        
    # 配置 CMake 项目
    - name: 配置 CMake
      run: |
        cd build
        cmake .. -G "Visual Studio 17 2022" -A Win32 -DCMAKE_BUILD_TYPE=Release
        
    # 构建项目
    - name: 构建项目
      run: |
        cd build
        cmake --build . --config Release --parallel 4
        
    # 检查构建结果
    - name: 检查构建结果
      run: |
        if (Test-Path "build\bin\Release\Stardew_Valley_Cocos2d-x4.0.exe") {
          Write-Host "✅ 构建成功! 可执行文件已生成"
          Get-ChildItem build\bin\Release\ -Recurse
        } else {
          Write-Host "❌ 构建失败! 未找到可执行文件"
          Get-ChildItem build\ -Recurse
          exit 1
        }
        
    # 上传构建产物
    - name: 上传构建产物
      uses: actions/upload-artifact@v4
      if: success()
      with:
        name: windows-release-build
        path: |
          build/bin/Release/
          build/lib/Release/
          Resources/
        retention-days: 7
        
  # Windows Debug 构建任务
  windows-debug-build:
    runs-on: windows-2022
    if: github.event_name == 'workflow_dispatch' || contains(github.event.head_commit.message, '[debug]')
    
    steps:
    - name: 检出代码
      uses: actions/checkout@v4
      with:
        submodules: recursive
        fetch-depth: 0
        
    - name: 设置 Python 2.7
      uses: actions/setup-python@v4
      with:
        python-version: '2.7'
      continue-on-error: true
      id: python27_debug
        
    - name: 设置 Python 3（备用方案）
      if: steps.python27_debug.outcome == 'failure'
      uses: actions/setup-python@v4
      with:
        python-version: '3.8'
        
    - name: 设置 Visual Studio 开发环境
      uses: microsoft/setup-msbuild@v1.3
      with:
        vs-version: '17.0'
        
    - name: 修改 download-deps.py 支持 Python 3（Debug）
      if: steps.python27_debug.outcome == 'failure'
      run: |
        cd cocos2d
        copy download-deps.py download-deps.py.backup
        (Get-Content download-deps.py) | ForEach-Object {
          if ($_ -match "if not _check_python_version") {
            "    # if not _check_python_version():"
          } elseif ($_ -match "exit\(\)") {
            "    #     exit()"
          } else {
            $_
          }
        } | Set-Content download-deps.py
      shell: powershell
        
    - name: 下载 cocos2d-x 依赖
      run: |
        cd cocos2d
        python download-deps.py --r no
        
    - name: 创建构建目录
      run: mkdir build-debug
        
    - name: 配置 CMake (Debug)
      run: |
        cd build-debug
        cmake .. -G "Visual Studio 17 2022" -A Win32 -DCMAKE_BUILD_TYPE=Debug
        
    - name: 构建项目 (Debug)
      run: |
        cd build-debug
        cmake --build . --config Debug --parallel 4
        
    - name: 上传 Debug 构建产物
      uses: actions/upload-artifact@v4
      if: success()
      with:
        name: windows-debug-build
        path: |
          build-debug/bin/Debug/
          build-debug/lib/Debug/
          Resources/
        retention-days: 7
        
  # 构建报告任务
  build-report:
    runs-on: ubuntu-latest
    needs: [windows-build]
    if: always()
    
    steps:
    - name: 构建结果报告
      run: |
        echo "## 🎮 Stardew Valley Cocos2d-x 构建报告" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "| 构建任务 | 状态 |" >> $GITHUB_STEP_SUMMARY
        echo "|---------|------|" >> $GITHUB_STEP_SUMMARY
        echo "| Windows Release | ${{ needs.windows-build.result == 'success' && '✅ 成功' || '❌ 失败' }} |" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "### 📦 构建产物" >> $GITHUB_STEP_SUMMARY
        echo "- Windows Release 版本已上传为构建产物" >> $GITHUB_STEP_SUMMARY
        echo "- 包含游戏可执行文件和所需的资源文件" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "### 🚀 运行说明" >> $GITHUB_STEP_SUMMARY
        echo "1. 下载 windows-release-build 构建产物" >> $GITHUB_STEP_SUMMARY
        echo "2. 解压到本地目录" >> $GITHUB_STEP_SUMMARY
        echo "3. 运行 Stardew_Valley_Cocos2d-x4.0.exe" >> $GITHUB_STEP_SUMMARY
